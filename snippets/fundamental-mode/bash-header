# -*- mode: snippet -*-
# contributor: sancoder.q@gmail.com
# name: bash-header
# key: bash-header
# --

#!/bin/bash

set -o errexit
set -o nounset
set -o pipefail

SOURCE_DIR="$( cd "$( dirname "\${BASH_SOURCE[0]}" )" && pwd )"
ROOT_DIR="\$SOURCE_DIR/.."
